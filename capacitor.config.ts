import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.timweb.budgettracker',
  appName: 'budget-tracker',
  webDir: 'dist'
};

export default config;
